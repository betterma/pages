{
  "name": "fake-chat",
  "private": true,
  "version": "1.0.0",
  "description": "Fake chat mobile web + Android APK shell (Capacitor)",
  "scripts": {
    "copy-www": "node scripts/copy-www.js",
    "cap:sync": "npm run copy-www && npx cap sync android",
    "cap:open": "npx cap open android",
    "apk:debug": "npm run cap:sync && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.2",
    "@capacitor/core": "^7.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.2"
  }
}
